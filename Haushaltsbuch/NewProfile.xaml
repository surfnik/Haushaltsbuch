<Window x:Class="Haushaltsbuch.NewProfile"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Haushaltsbuch"
        mc:Ignorable="d"
        Title="Neues Profil" Width="350" Height="260">
    <StackPanel Margin="20,0,20,20">

        <Label Content="Bitte Namen eingeben:" Margin="0,10,0,0"/>
        <TextBox x:Name="txtName" Margin="0,5,0,0" />
        <Label Content="Bitte Kontostand eingeben" Margin="0,10,0,0" />
        <TextBox x:Name="txtAmmount" Margin="0,5,0,0"/>

        <Grid Margin="0,22,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="50" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button x:Name="btnOk" Content="OK" Grid.Column="1" Height="20" Click="btnOk_Click" />
            <Button x:Name="btnCancel"  Content="Abbrechen" Grid.Column="3" IsCancel="True" Click="btnCancel_Click"/>
        </Grid>
    </StackPanel>
</Window>
